[build-system]
requires = ["setuptools>=61.0", "setuptools-scm>=8.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cautogui"
description = "High-performance GUI automation with C++ core"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [{name = "danckard"}]
dynamic = ["version", "urls"]

[tool.setuptools_scm]